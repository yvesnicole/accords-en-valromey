# CLAUDE.md — Accords en Valromey

> Festival de musique classique — Champagne-en-Valromey, Ain
> Site statique bilingue FR/EN

## Stack

| Layer | Tech | Version |
|-------|------|---------|
| SSG | Astro | 5.x |
| CMS | TinaCMS | 3.8+ (vanilla-JS bridge, no React) |
| Styling | Tailwind CSS | v4 (via `@tailwindcss/vite`) |
| i18n | astro-i18n-aut | 0.7.x (SSG-only) |
| Deploy | Fly.io | Paris (cdg), Docker multi-stage, Nginx |
| CI | GitHub Actions | `deploy.yml` (NOT ACTIVE — deploy manually) |

## Build & Dev

```bash
npm run dev      # tinacms dev -c "astro dev"  → localhost:4321
npm run build    # tinacms build --content=local --skip-cloud-checks && astro build
npm run preview  # astro preview (serve dist/)
npx astro check  # type checking
```

**Build gotchas**:
- `--skip-cloud-checks` is REQUIRED for local/CI builds without Tina Cloud
- `NODE_OPTIONS="--max-old-space-size=4096"` in Dockerfile prevents OOM during Tina build
- `tina/__generated__/` MUST be committed to git (required for Tina Cloud schema detection)
- `tina/tina-lock.json` must be committed (generated by `tinacms dev`, not `tinacms build`)

## Environment Variables

```
PUBLIC_TINA_CLIENT_ID=   # Tina Cloud client ID (public, embedded in build)
TINA_TOKEN=              # Tina Cloud token (secret, build-time only)
```

Both flow through GitHub Actions secrets → Docker build args → `npm run build`.

## Content Architecture

**TinaCMS owns all content.** Astro's content layer is a stub (`src/content.config.ts` loads a non-existent `.json` pattern just to suppress warnings about orphaned directories).

### Content Collections (defined in `tina/config.ts`)

| Collection | Path | Fields |
|---|---|---|
| `concert` | `src/content/concerts/{fr,en}/*.mdx` | title, date, locale, translationKey, program (rich-text), description, image, helloAssoLink |
| `musician` | `src/content/musicians/{fr,en}/*.mdx` | name, locale, translationKey, instrument, photo, photoCredit, bio (rich-text) |
| `page` | `src/content/pages/{fr,en}/*.mdx` | title, locale, translationKey, subtitle, body (rich-text) |

### Bilingual Content Pattern

Every logical document has **two MDX files** (one per locale) linked by a shared `translationKey`:

```
src/content/concerts/
  fr/opening-2026.mdx    → locale: "fr", translationKey: "opening-2026"
  en/opening-2026.mdx    → locale: "en", translationKey: "opening-2026"
```

**Data fetching** uses `src/lib/data.ts` which wraps the TinaCMS generated client:
```typescript
import { getConcerts, getConcert, getMusicians, getPage } from '../lib/data';
const concerts = await getConcerts('fr'); // auto-sorted by date
const opening = await getConcert('opening-2026', 'fr');
```

### ⚠️ Current Architectural Gap

The `src/lib/data.ts` fetchers exist and TinaCMS content (MDX files) exists, but **most pages still use hardcoded inline data**. Connecting pages to the data layer is the highest-value improvement.

## i18n System

### How It Works

- **FR is default** (root path `/`), **EN is under `/en/`**
- **Parallel page files**: `src/pages/billetterie.astro` (FR) and `src/pages/en/tickets.astro` (EN)
- Route slugs are mapped in `src/i18n/utils.ts` → `translateSlug()`
- UI strings in `src/i18n/ui.ts` → `ui.fr` / `ui.en` dictionaries (36 keys)
- Language switcher in `Header.astro` + `LanguageSwitcher.astro`

### Adding a New i18n Key

1. Add to `src/i18n/ui.ts` under both `fr` and `en` objects
2. TypeScript auto-infers the key via `type UiKey = keyof typeof ui.en`
3. Use in pages: `const t = useTranslations(locale); t('my.new.key')`

### Current i18n Issues

- **Footer is FR-only** (hardcoded French links, no translations)
- **Some pages hardcode strings** instead of using `useTranslations()` (festival, program, musicians pages)
- Ticket pricing on EN pages has updated prices that differ from FR pages (FR: 24€/45€/63€, EN: 21€/38€/48€)

### Route Slug Map

| FR | EN |
|----|----|
| `/` | `/en/` |
| `/le-festival/` | `/en/the-festival/` |
| `/programmation/` | `/en/program/` |
| `/musiciens/` | `/en/musicians/` |
| `/billetterie/` | `/en/tickets/` |
| `/contact/` | `/en/contact/` |
| `/soutenir/` | `/en/support-us/` |

## Design System

### Typography
- **Headings**: Playfair Display (serif) — 400, 600, 700
- **Body**: Inter (sans-serif) — 400, 600

### Colors (Tailwind v4 `@theme` tokens in `src/styles/global.css`)
- `--color-sage: #C4E0C4` — primary accent (use sparingly)
- `--color-ivory: #F9FFF7` — background
- `--color-contrast: #FFFFFF` — card backgrounds, contrast zones

### Custom Tokens
- `--spacing-section: 6rem` — vertical section spacing
- `--radius-card: 0.75rem` — card border radius
- `--font-serif`, `--font-sans` — font family tokens

### Tailwind v4 Notes
- **No `tailwind.config.*` file exists** — uses `@tailwindcss/vite` plugin + `@theme` in CSS
- Use `bg-ivory`, `text-sage`, `font-serif`, `font-sans`, `rounded-card`, `py-section` as utility classes

## Project Structure

```
src/
├── components/
│   ├── concerts/     ConcertCard, ConcertCarousel, ConcertList
│   ├── home/         Hero, FestivalIntro, NextConcert
│   ├── layout/       Header, Footer, LanguageSwitcher
│   ├── musicians/    MusicianGrid, MusicianCard, MusicianBio
│   ├── tickets/      PricingCard, HelloAssoButton
│   └── ui/           Button, Card, Badge, SectionTitle
├── content/          TinaCMS MDX content (not Astro content)
├── i18n/             ui.ts (dictionary), utils.ts (routing/detection)
├── layouts/          BaseLayout.astro (root HTML shell)
├── lib/              data.ts (TinaCMS client wrappers)
├── pages/            Route pages (FR root + /en/ subdir)
└── styles/           global.css (Tailwind @theme + fonts)
```

## Deployment

- **Platform**: Fly.io, region `cdg` (Paris)
- **Config**: `fly.toml` — 256MB RAM, 1 shared CPU, auto-stop on idle
- **Docker**: 3-stage build → `node:22-alpine` (deps + builder) → `nginx:alpine` (runtime)
- **Nginx**: gzip on, `/_astro/` cached 1y (immutable), static assets 30d, `try_files` for clean URLs
- **SSL**: Force HTTPS, auto-managed by Fly.io

### ⚠️ GitHub Actions NOT Active — Deploy Manually

GitHub Actions (`deploy.yml`) is configured but **not active**. Do NOT assume push-to-deploy works. After every commit, deploy **manually** with:

```bash
fly deploy --app accords-en-valromey \
  --build-arg PUBLIC_TINA_CLIENT_ID=<from .env> \
  --build-arg TINA_TOKEN=<from .env>
```

The Tina credentials are in `.env` at the project root (`PUBLIC_TINA_CLIENT_ID` and `TINA_TOKEN`). Without them, `tinacms build` fails with `ERR_MISSING_CLOUD_CREDS`.

## Common Gotchas

1. **`trailingSlash: 'always'`** — All internal links MUST end with `/`. Missing trailing slash = broken link in production.
2. **`content.config.ts` is a stub** — Don't use `astro:content` to query data. Use `src/lib/data.ts` which wraps the Tina client.
3. **EN pages have their own `.astro` files** — They're NOT dynamically generated. `src/pages/en/tickets.astro` is a separate file from `src/pages/billetterie.astro`.
4. **Tailwind v4 `@theme`** — Custom tokens are defined in `src/styles/global.css`, NOT in a config file.
5. **Tina `tina-lock.json`** — Commit it. If missing, run `tinacms dev` first to generate it.
6. **`@/` alias** — Maps to `src/`. Use `import X from '@/components/...'`.
7. **HelloAsso links** — External ticketing platform. Links are stored as `helloAssoLink` field on concerts. Don't build a custom checkout.
8. **GitHub Actions NOT active** — `git push` does NOT trigger deploy. Always deploy manually with `fly deploy --app accords-en-valromey --build-arg PUBLIC_TINA_CLIENT_ID=... --build-arg TINA_TOKEN=...`. Credentials in `.env`.
9. **`fly deploy` needs --build-arg** — Without `PUBLIC_TINA_CLIENT_ID` and `TINA_TOKEN` build args, `tinacms build` fails with `ERR_MISSING_CLOUD_CREDS`.

## When Adding Features

1. **New page?** → Create FR `.astro` + EN `.astro` + nav entry in Header + route mapping (if slug differs) + i18n keys + optional TinaCMS content
2. **New TinaCMS field?** → Edit `tina/config.ts`, run `tinacms dev` to regenerate types, commit `tina-lock.json`
3. **New component?** → Follow existing patterns: `interface Props`, pure Astro (no React), inline `<script>` for vanilla JS interactivity
4. **New i18n key?** → Add to both `fr` and `en` in `src/i18n/ui.ts`, use `t('key.name')` in pages
5. **Styling?** → Use Tailwind utility classes + custom tokens (`bg-sage`, `text-ivory`, `font-serif`, `rounded-card`, `py-section`)
